module body Test is
    record Foo: Linear is
    end;

    function main(): ExitCode is
        let f: Foo := Foo();

        borrow ref: &[Foo, Reg] := &f do
        end borrow;

        let {} := f;
        return ExitSuccess();
    end;
end module body.
